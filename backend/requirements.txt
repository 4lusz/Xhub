# Correcao (auditoria de seguranca -- dependencias vulneraveis, etapa 2):
# `fastapi==0.115.6` fixava `starlette==0.41.3` transitivamente, com
# varios CVEs conhecidos sem correcao disponivel dentro daquela faixa de
# compatibilidade (`starlette<0.42.0`). Atualizado para uma versao que
# permite a faixa `starlette>=0.46.0` -- resolvido para `starlette`
# 1.3.1 (a mais recente disponivel), que corrige todos os CVEs
# encontrados. Validado com a suite completa + smoke test manual de
# CORS/headers/rate-limit/body-size/upload/JWT/exceptions apos a
# atualizacao (nao apenas `pytest`, dado o tamanho do salto de versao).
fastapi==0.136.0
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
# Correcao (auditoria de seguranca -- dependencias vulneraveis): versoes
# anteriores tinham CVEs conhecidos e corrigidos upstream sem exigir
# mudanca de API para o uso restrito que este projeto faz de cada lib
# (JWT HS256 simples via jose; Fernet/AES via cryptography). Bumps de
# patch/minor dentro da mesma linha de compatibilidade, validados com a
# suite completa + smoke test manual de login/JWT/upload apos a
# atualizacao -- nunca a ultima versao "bleeding edge" sem necessidade.
python-jose[cryptography]==3.4.0
cryptography==48.0.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32
httpx==0.28.1
apscheduler==3.10.4
python-dotenv==1.2.2

# Testes (backend/tests/, ver README.md/claude.md -- "docker compose exec
# backend pytest"). Sem esta dependencia declarada, uma imagem construida
# do zero (`docker compose up --build`) nao consegue rodar a suite.
pytest==8.3.4
